1. Опишите вычислительный эксперимент, цель которого – определить эффективность параллельной реализации алгоритма std::sort()
	1. Узнаём поддерживаемое кол-во потоков std::thread::hardware_concurrency()
	2. Тестируем увеличивая на порядок размерность, пока эффективность не перестанет значительно изменяться.
	3. Можно проверить на нескольких вариантах - отсортированный вектор, случайно сгенерированный (только одинаковые), отсортированный в обратном порядке.

2. 3 алгоритма и большая задача